# API documentation

This directory is reserved for documentation of the future application API. The current server provides only `GET /health` for local scaffold verification; it contains no application endpoints or business logic.
